tasks:
  Setup:
    shell: python -m pip install --upgrade pip && pip install -r requirements.txt 

  Extract News Data:
    shell: python news_scrapper.py

condaConfigFile: conda.yaml

artifactsDir: output