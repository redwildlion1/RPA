tasks:
  Extract News Data:
    robot_run:
      command: robot
      args:
        - --report NONE
        - --outputdir .
        - --log log.html
        - tasks.robot
artifactsDir: ./output
conda:
  file: conda.yaml